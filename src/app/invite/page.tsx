"use client";
import { useEffect, useState } from "react";
export default function InvitePage(){
  const [token,setToken]=useState(""); const [name,setName]=useState(""); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); const [done,setDone]=useState(false);
  useEffect(()=>setToken(new URLSearchParams(window.location.search).get("token")||""),[]);
  const submit=async(e:React.FormEvent)=>{e.preventDefault();const response=await fetch('/api/invites/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,displayName:name,password})});const data=await response.json();setMessage(data.success?'Your pilot account is ready. You can now sign in.':data.error);setDone(Boolean(data.success));};
  return <main className="min-h-screen bg-[#F7F5F0] px-5 py-12 flex items-center justify-center"><section className="w-full max-w-md rounded-[28px] border border-slate-200 bg-white p-7 shadow-xl"><img src="/logoextenso.png" alt="DevKiller" className="h-9"/><h1 className="mt-8 text-2xl font-extrabold">Accept your pilot invitation</h1><p className="mt-2 text-sm text-slate-500">Create your private DevKiller access.</p>{done?<a href="/login" className="mt-7 block rounded-xl bg-[#111420] px-4 py-3 text-center text-sm font-bold text-white">Continue to sign in</a>:<form onSubmit={submit} className="mt-7 space-y-4"><input required placeholder="Your name" value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-xl border px-3.5 py-3 text-sm"/><input required type="password" minLength={12} placeholder="Password · at least 12 characters" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border px-3.5 py-3 text-sm"/><button className="w-full rounded-xl bg-[#FF4B72] px-4 py-3 text-sm font-bold text-white">Create pilot account</button></form>}{message&&<p className="mt-4 text-xs font-semibold text-slate-600">{message}</p>}</section></main>;
}
