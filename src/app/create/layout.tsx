import {pageMetadata} from '@/lib/site-metadata';
export const metadata=pageMetadata('DK Create — Build your next app','Describe your idea, create an app and refine it in your DevKiller workspace.','/create',false);
export default function Layout({children}:{children:React.ReactNode}){return children;}
