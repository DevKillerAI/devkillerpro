import {pageMetadata} from '@/lib/site-metadata';
export const metadata=pageMetadata('Sign in to DevKiller','Access DK Tools projects and DK Create with one DevKiller account.','/login',false);
export default function Layout({children}:{children:React.ReactNode}){return children;}
