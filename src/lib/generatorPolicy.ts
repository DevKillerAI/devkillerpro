/** Shared display/admission policy. Reservations are enforced transactionally in PostgreSQL. */
export const WORKBENCH_RUN_MAX_COST_MICROS = 1_000_000;
export const WORKBENCH_RUN_MAX_PROVIDER_CALLS = 5;
